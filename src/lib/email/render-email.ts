// Simple email renderer
// In production, use @react-email/render for React-based templates
export function renderEmailHtml(template: string, _data: Record<string, unknown> = {}): string {
  return template;
}
